# Use a base image
FROM ubuntu:latest

# Set the working directory
WORKDIR /app

# Copy all folders and their programs
COPY . /app

# Add any necessary installation commands here

# Specify the default command to run when the container starts
CMD ["/bin/bash"]

